version: '3'
services:
    backend:
        image: gitlab-registry.cern.ch/mmetzler/mtg-web
        networks:
            - backend

    frontend:
        image: gitlab-registry.cern.ch/mmetzler/mtg-web/static
        ports:
            - "80:80"
        networks:
            - frontend
            - backend

networks:
    frontend:
    backend: